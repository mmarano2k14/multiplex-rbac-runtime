namespace Multiplexed.Abstractions.AI.Rag.Data
{
    /// <summary>
    /// Defines how a RAG query datasource should execute.
    /// </summary>
    public enum RagQueryExecutionMode
    {
        Direct = 0,
        Provider = 1
    }
}